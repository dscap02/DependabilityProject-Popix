# ==========================================
# Pop!x - Environment configuration template
#
# Copy this file to ".env" and customize values:
#   cp .env.example .env
#
# The .env file must NOT be committed.
# ==========================================

# --- MySQL configuration (database container) ---
MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_DATABASE=Popix
MYSQL_USER=popix
MYSQL_PASSWORD=changeme_pass

# --- Application configuration (Tomcat container) ---
# Use "db" as host (docker-compose service name)
POPIX_DB_URL=jdbc:mysql://db:3306/Popix?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
